<%# 商品登録フォーム %>
<%= form_with model: @plan_tag, url: plans_path, method: :post, local: true do |f| %>
  かて<%= f.text_area :category %>
  item<%= f.text_area :item %>
  <%= f.datetime_select :start_time %>
  おす<%= f.submit %>
<% end %>

<% @plans.each do |plan| %>
  <%= plan.item %>
<% end %>





<%# カレンダー表示 %>
<%= month_calendar do |date| %>
  <%= date.day %>
<% end %>

