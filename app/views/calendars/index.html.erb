<%# 商品登録フォーム %>
<%= form_with model: [@calendar, @item], local: true do |f| %>
  かて<%= f.text_area :category %>
  メモ<%= f.text_area :memo %>
  おす<%= f.submit %>
<% end %>





<%# カレンダー表示 %>
<%= month_calendar do |date| %>
  <%= date.day %>
<% end %>

